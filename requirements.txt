# curateur - ScreenScraper ROM Metadata & Media Scraper
# Python package dependencies

# Core dependencies
PyYAML>=6.0
lxml>=4.9.0
httpx>=0.24.0
Pillow>=10.0.0
rich>=14.2.0  # Latest stable release
psutil>=5.9.0

# Development dependencies (optional)
# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0
respx>=0.20.0  # HTTP mocking for httpx (replaces responses)
