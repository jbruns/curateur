screenscraper:
  user_id: "test_user"
  user_password: "test_password"

paths:
  roms: ./roms
  media: ./media
  gamelists: ./gamelists
  es_systems: ./systems.xml

api:
  request_timeout: -5
  max_retries: -1
  retry_backoff_seconds: -10
