screenscraper:
  user_id: "test_user"
  user_password: "test_password"

paths:
  roms: ./roms
  media: ./media
  gamelists: ./gamelists
  es_systems: ./systems.xml

scraping:
  systems: "should be a list"
  skip_scraped: "not a boolean"
  image_min_dimension: "not an integer"
  
api:
  max_retries: 3.5
